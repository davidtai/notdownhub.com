# notdownhub.com

Marketing site for **notdownhub** — the CI runner that doesn't go down.

Static Vite + TypeScript + Tailwind. Build with `npm install && npm run build`;
deploy the `dist/` folder to any static host. (Full docs land with the site.)
